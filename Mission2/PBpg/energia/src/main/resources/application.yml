spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/java_gip346c #conecta a la base de datos: el nombre es igual a la DB
    username: postgres
    password: 119326
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8080 #El puerto por el cual corre 
  error:
    include-message: always #Incluye el error por defecto si no se encuentra el usuario 